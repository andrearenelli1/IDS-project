\documentclass[journal,10pt,twocolumn]{IEEEtran}

% ---- Packages ----
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{amsmath, amssymb}
\usepackage{bm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{hyperref}
\usepackage{cite}

\hypersetup{colorlinks=false}

% ============================================================
\title{Distributed Multi-Drone ARTVA Search \& Rescue\\
       over Real Alpine Terrain in GPS-denied conditions}

\author{Andrea~Renelli,~\IEEEmembership{Student,~University~of~Trento}
        and~Luca~Zanolini,~\IEEEmembership{Student,~University~of~Trento}%
\thanks{University of Trento, Italy.
  \texttt{\{andrea.renelli, luca.zanolini-1\}@studenti.unitn.it}.}
\thanks{This report is submitted as the final project for the course
  ``Intelligent Distributed Systems''.}}

% ============================================================
\begin{document}

\maketitle

% ============================================================
\begin{abstract}
When an avalanche strikes, survival odds collapse within minutes—and
every second of search counts.
We present a fully distributed multi-drone system that localizes a
buried ARTVA beacon with no central coordinator, no shared map, and no
external positioning: each drone acts on local state alone and talks
only to peers within radio range.
%
The entire system is evaluated under full GPS denial—the worst-case
scenario for self-localization—where each drone relies solely on
onboard inertial integration corrected by cooperative UWB ranging and
LiDAR altitude (IMDCL).  This is a deliberate stress test: the same
framework transparently accommodates a GPS measurement whenever one
is available.  It suffices for a single drone to carry a GPS receiver
and remain on average connected to the rest of the fleet for the
absolute fix to propagate through the cooperative filter, eliminating
positional drift across all agents without any change to the
architecture.
%
Each agent runs a four-state machine—\textsc{Search},
\textsc{Track}, \textsc{Stop}, \textsc{Support}—driven by an MPC
controller, with Extremum Seeking guiding it up the signal gradient and
a distributed Particle Filter fusing every nearby measurement into a
sharp 3-D victim estimate.
%
Validated over [XXXX] experiments on real alpine terrain, spanning fleet
size, ARTVA noise, communication radius, and burial depth, the system
delivers reliable sub-metre localization—proving that coordinated
autonomous rescue is achievable under the harshest realistic conditions.
\end{abstract}

\begin{IEEEkeywords}
Multi-drone systems, ARTVA search and rescue, Model Predictive Control,
Extremum Seeking, IMDCL cooperative localization, particle filter,
avalanche rescue.
\end{IEEEkeywords}

% ============================================================
\section{Introduction}
\label{sec:intro}

Avalanche burial is one of the most time-critical rescue scenarios:
survival probability exceeds 90\,\% in the first 15 minutes and drops
to below 30\,\% after 35 minutes~\cite{Falk2011}.
The ARTVA (Apparecchio di Ricerca dei Travolti in Valanga) standard
transmits a 457\,kHz magnetic field; a drone fleet equipped with
receiving antennas can sweep the burial zone far faster than human
searchers.

The core challenge is to coordinate multiple UAVs
without a central server, localize a buried beacon from its weak
magnetic dipole signal, and deliver a precise 3-D position estimate
to ground rescuers—all under GPS denial (avalanche terrain may block
satellite signals) and tight real-time constraints.

We deliberately design and evaluate the system for the
\emph{worst-case} scenario, full GPS denial, in which no agent has
access to any absolute position reference.  This is the hardest
operating condition: without an external anchor, each drone's position
estimate drifts unbounded as odometric error accumulates.  The
cooperative IMDCL filter mitigates this by fusing inter-drone UWB
ranging and LiDAR altitude, keeping the fleet's relative geometry
accurate enough to localize the victim even in this degraded regime.
Importantly, the design degrades gracefully in the opposite direction:
it is sufficient for a \emph{single} agent to regain GPS—provided it
remains, on average, connected to the rest of the team—for that
absolute reference to propagate through the cooperative filter and
eliminate the drift across the entire fleet.  The method therefore
operates fully without GPS, while transparently exploiting any absolute
fix that becomes available.

\subsection*{Problem Formulation}

Given a team of $N$ UAVs deployed over an alpine search area with
unknown victim position $\bm{\theta} = [x_v, y_v, z_v]^\top$, the
goal is to:
(i) detect the ARTVA beacon as quickly as possible,
(ii) cooperatively estimate $\bm{\theta}$ within a target accuracy
sufficient for ground rescuers to locate the burial point, and
(iii) do so without any central coordinator, operating only with
local state and bounded peer-to-peer communication under GPS denial.

This work contributes:
\begin{itemize}
  \item A four-state FSM with fully arrival-gated waypoint management,
        combining a coverage lawnmower (\textsc{Search}), Extremum
        Seeking source tracking (\textsc{Track}), cooperative hover
        (\textsc{Stop}), and cooperative orbit (\textsc{Support}).
  \item Dynamic detection thresholds with a physics-based floor,
        derived at runtime from a distributed noise calibration step.
  \item A 3-D cooperative Particle Filter for victim localization,
        initialized from the ARTVA dipole model in polar coordinates,
        constrained below the LiDAR-sensed terrain surface, and updated
        cooperatively from all drones in range.
  \item Full simulation over real TINItaly DEM tiles, with a
        parametric sweep across terrain patches, fleet sizes, noise
        levels, and communication radii.
\end{itemize}

% ============================================================
\section{Adopted Models}
\label{sec:models}

\subsection{Communication System}
\label{sec:comm}

The drone network is modeled as a time-varying undirected graph
$\mathcal{G}(t) = (\mathcal{V}, \mathcal{E}(t))$, where
$\mathcal{V} = \{1, \ldots, N\}$ is the set of agents and
$(i,j) \in \mathcal{E}(t)$ if and only if
$\|\mathbf{p}_i(t) - \mathbf{p}_j(t)\| < r_c$.
No broadcast infrastructure exists; all inter-agent communication is
peer-to-peer within the communication radius $r_c$.

Two distributed protocols run over this graph:

\paragraph{Average-consensus (noise calibration)}
At startup, each drone takes $N_{\text{cal}} = 20$ ARTVA samples at
its initial position.  An average-consensus protocol over
$\mathcal{G}(0)$ aggregates all measurements into a team-wide noise
estimate $(\hat{\mu}, \hat{\sigma})$ before any flight begins.

\paragraph{Min-consensus (partner selection)}
When a drone $h$ enters \textsc{Stop}, a BFS from $h$ first identifies
the connected component $\mathcal{C}(h)$ of reachable drones; agents
outside it are excluded.  A min-consensus flood then propagates
synchronously for up to $k_{\max} = 10$ rounds: newly-informed drones
record their distance $\|\hat{\mathbf{p}}_i - \hat{\mathbf{p}}_h\|$ and
each applies a min-update over its neighbors.  Convergence is guaranteed
in at most $\mathrm{diam}(\mathcal{G}[\mathcal{C}(h)])$ rounds, after
which the two closest available drones are recruited as \textsc{Support}
partners.

UWB radio is used for ranging (inter-drone distance measurements fed
into IMDCL) and for piggy-backing lightweight state messages
(position estimate, ARTVA reading, FSM state).

\subsection{System Model}
\label{sec:sysmodel}

\subsubsection{Drone dynamics}

Since the goal of this work is a proof of concept for the whole
distributed framework, each drone is modeled as a 3-D point mass
controlled in acceleration.  This abstraction is justified on two
grounds: first, the ARTVA signal depends only on position and not on
orientation, so attitude dynamics do not affect the measurement model;
second, the point-mass model can be treated as a black box hiding a
lower-level controller that, given the desired acceleration from the
high-level MPC, computes the actual motor commands, as is standard in
quadrotor control architectures.  States and inputs are:
\begin{equation}
  \mathbf{x} = [p_x, p_y, p_z, v_x, v_y, v_z]^\top \in \mathbb{R}^6,
  \quad
  \mathbf{u} = [a_x, a_y, a_z]^\top \in \mathbb{R}^3
  \label{eq:dynamics}
\end{equation}
subject to acceleration bound $|\mathbf{a}| \leq 6$\,m/s$^2$ and
speed bound $|\mathbf{v}| \leq 3$\,m/s.

The continuous dynamics $\dot{\mathbf{x}} = A_c \mathbf{x} + B_c
\mathbf{u}$ are discretized with a Zero-Order Hold (step
$\Delta t = 0.1$\,s), assuming $\mathbf{u}$ constant over each interval.
The matrix exponential resolves in closed form, yielding an exact
discretization:
\begin{equation}
  \mathbf{x}(k+1) = F\,\mathbf{x}(k) + G\,\mathbf{u}(k) +
  G\,\bm{\eta}(k)
  \label{eq:zoh}
\end{equation}
where $\bm{\eta} \sim \mathcal{N}(0, Q)$ is isotropic process noise
with standard deviation $\sigma_{\text{acc}}$, and
\begin{equation}
  F = \begin{bmatrix} I_3 & \Delta t\,I_3 \\ 0_3 & I_3 \end{bmatrix},
  \quad
  G = \begin{bmatrix} \tfrac{1}{2}\Delta t^2 I_3 \\ \Delta t\,I_3
  \end{bmatrix},
  \quad
  Q = \sigma_{\text{acc}} I_3.
\end{equation}

The drone maintains a constant AGL altitude of $h_{\text{AGL}} =
1.5$\,m; terrain height is read from the TINItaly DEM at runtime.

\subsubsection{ARTVA signal model}

The buried transmitter is modeled as a magnetic dipole aligned with
gravity.  The received power at position $\mathbf{p}$ from a source at
$\bm{\theta} = [x_v, y_v, z_v]^\top$ is

\begin{equation}
  S(\mathbf{p}) = \frac{m\,\sqrt{1 + 3\cos^2\!\psi}}{\|\mathbf{p} -
  \bm{\theta}\|^3}
  \label{eq:artva}
\end{equation}

where $m$ is the normalized magnetic moment and $\cos\psi =
-(\mathbf{p} - \bm{\theta})_z / \|\mathbf{p} - \bm{\theta}\|$.
An additive Gaussian noise $\mathcal{N}(0, \sigma_n^2)$ is
superimposed.  Signal measurements are low-pass filtered with a sliding
batch window of $N_{\text{sig}} = 5$ samples to reduce noise influence
on state transitions.

The dipole moment $m$ is a dimensionless scaling factor that sets the
unit of $S$; since all signal thresholds and noise levels are expressed
in the same units, its absolute value does not affect system behaviour
(we take $m = 1$).
The transmitter is assumed aligned with the world vertical axis.  In
general a buried victim may lie in any orientation, making the dipole
axis unknown; when the axis is not vertical the signal maximum shifts
laterally from the true position~\cite{Azzollini2021}.  This is not a
limitation specific to the proposed approach: any search strategy
converges to the point of maximum signal, which is exactly where the
rescuer begins excavation, i.e.\ the same reference a hand-held ARTVA
search would provide.  The rescuer then refines the position empirically
as digging proceeds and the signal strengthens.

% ============================================================
\section{Solution}
\label{sec:solution}

\subsection{Finite State Machine}
\label{sec:fsm}

Each drone cycles through four states (Fig.~\ref{fig:topview}):
\begin{list}{}{\setlength{\labelwidth}{1.6cm}%
               \setlength{\leftmargin}{1.8cm}%
               \setlength{\labelsep}{0.2cm}%
               \setlength{\itemsep}{2pt}}
  \item[\textsc{Search}] Lawnmower coverage; transitions to
        \textsc{Track} when the filtered ARTVA signal exceeds
        $\tau_{\text{detect}}$.
  \item[\textsc{Track}] Extremum Seeking navigation;
        transitions to \textsc{Stop} when signal $\geq
        \tau_{\text{stop}}$.
  \item[\textsc{Stop}] Hovers at current position; recruits two
        partners via min-consensus; the Particle Filter continues
        to refine the victim estimate.
  \item[\textsc{Support}] Orbits the \textsc{Stop} drone on a circle
        of radius $r_{\text{orbit}}$, one clockwise, one
        counter-clockwise; transitions to \textsc{Stop} when its own
        signal exceeds $\tau_{\text{stop}}$.
\end{list}
The mission terminates when $N_{\text{stop}} \geq 3$ drones are in
\textsc{Stop} state and the particle filter estimate lies within 10\,m
of the true victim.

\begin{figure}[tb]
  \centering
  \includegraphics[width=\columnwidth]{figures/trajectories_top_view}
  \caption{Top-view trajectory of a 3-drone mission on a real
  alpine DEM patch.  Colors encode FSM state: blue = Search, orange =
  Track, red = Stop/Support.  Victim position marked with a star;
  final PF estimate shown as a cross.}
  \label{fig:topview}
\end{figure}

\subsection{Dynamic Detection Thresholds}
\label{sec:thresholds}

Fixed thresholds would either miss the source under high noise or
trigger ES outside its convergence basin under very low noise.
Using the team-wide noise estimate from consensus, the thresholds are:

\begin{align}
  \tau_{\text{detect}} &= \max\!\left(
    \hat{\mu} + 5\hat{\sigma},\;
    \frac{m}{r_{\text{ES}}^3}
  \right)
  \label{eq:detect_thr}\\
  \tau_{\text{stop}} &= \frac{m}{r_{\text{found}}^3}
  \label{eq:stop_thr}
\end{align}

where $r_{\text{ES}} = 50$\,m is the maximum reliable ES convergence
radius~\cite{Azzollini2021} and $r_{\text{found}} = 10$\,m is the
target localization accuracy.  The physics-based floor prevents very
low noise from extending detection beyond the ES convergence basin.
At the nominal $\sigma_n = 10^{-6}$ the floor dominates
($5\hat{\sigma} = 5\times10^{-6} < m/r_{\text{ES}}^3 = 8\times10^{-6}$);
above $\sigma^* \approx 1.6\times10^{-6}$ the $5\hat{\sigma}$ arm takes
over and the detection SNR is clamped at 5.  The stop threshold is
purely geometric: no noise-exceedance term is needed, since by the time
a drone enters \textsc{Track} it has already crossed
$\tau_{\text{detect}}$, which acts as the upstream noise gate.  At the
nominal noise level this gives $\tau_{\text{stop}}/\hat{\sigma} =
10^3$.

\subsection{Search: Adaptive Lawnmower}
\label{sec:lawnmower}

In \textsc{Search}, each drone covers a vertical strip of the workspace
via a boustrophedon pattern.  Lane spacing is derived at runtime from
$\tau_{\text{detect}}$:

\begin{equation}
  r_{\text{detect}} = \left(\frac{m}{\tau_{\text{detect}}}\right)^{1/3},
  \quad
  n_{\text{lanes}} = \left\lceil \frac{W/N_d}{2\,r_{\text{detect}}}
  \right\rceil
  \label{eq:lanes}
\end{equation}

where $W$ is the workspace width and $N_d$ the number of drones.
A lower noise level produces a larger $r_{\text{detect}}$ and hence
fewer, wider lanes, reducing search time proportionally.
Lanes are assigned with stride $N_d$ (drone $i$ covers lanes $i,\,
i{+}N_d,\, i{+}2N_d, \ldots$), so that active drones always sweep
adjacent lanes, maximizing communication-graph connectivity throughout
the search phase and keeping neighbors in range for cooperative IMDCL
and PF updates.

\subsection{Track: Extremum Seeking}
\label{sec:es}
\begin{figure}[h]
    \centering
    \includegraphics[width=0.6\columnwidth]{figures/ES_example.png}
    \caption{ES trajectory example, image adapted from ~\cite{Azzollini2021}}
    \label{fig:ESexample}
\end{figure}
Upon detection, the drone switches to Extremum Seeking
(ES)~\cite{Azzollini2021}, a model-free algorithm that drives a
circular probe trajectory whose center gradient-ascends toward the
signal maximum.  Following equations (11)–(13) of~\cite{Azzollini2021}:

\begin{align}
  \dot{x}_{\text{ref}} &= \sqrt{\alpha\omega}\,\cos(\omega t +
  \kappa\,y_t) \label{eq:es_x}\\
  \dot{y}_{\text{ref}} &= \sqrt{\alpha\omega}\,\sin(\omega t +
  \kappa\,y_t) \label{eq:es_y}\\
  \dot{\alpha} &= \frac{\alpha_{\max} - \alpha}{\lambda}
  \label{eq:es_alpha}
\end{align}

where $y_t = 1/\!\sqrt[3]{S}$ is the conditioned signal (convex with
minimum at source), $\alpha$ ramps from 0 to $\alpha_{\max}=20$ with
time constant $\lambda = 15$\,s, and $\omega = V_{\max}^2/\alpha_{\max}
= 0.45$\,rad/s ensures $\sqrt{\alpha_{\max}\omega} = V_{\max} =
3$\,m/s.

\paragraph{Convergence and parameter choice}
Proposition 1 of~\cite{Azzollini2021} establishes $(1/\omega)$-SPUUB
convergence: for any target neighbourhood $\delta > 0$, a sufficiently
large $\kappa\alpha_{\max}$ guarantees convergence from any initial
condition in a corresponding domain of attraction, which can be
arbitrarily enlarged by increasing $\kappa\alpha_{\max}$ at the cost of
slower convergence.  The SITL evaluation of~\cite{Azzollini2021}
validated these parameters up to 50\,m from the source (the operating
range of commercial ARTVA receivers) with $\alpha_{\max} = 20$,
$\kappa = 0.07$, $\omega = 0.65$\,rad/s.  We retain $\alpha_{\max} = 20$
but lower $\omega$ to 0.45\,rad/s and $\kappa$ to 0.05 to satisfy our
tighter velocity ceiling (3\,m/s vs.\ 3.6\,m/s in~\cite{Azzollini2021}),
and take $r_{\text{ES}} = 50$\,m as the empirically validated
convergence range, used as the physical floor of the detection
threshold (Section~\ref{sec:thresholds}).

\subsection{Stop and Support: Cooperative Confirmation}
\label{sec:stop_support}

When a drone's signal reaches $\tau_{\text{stop}}$, it enters
\textsc{Stop} and recruits two partners via min-consensus.
Each partner is assigned a circular orbit:

\begin{equation}
  r_{\text{orbit}} = \left(\frac{m}{S_{\text{stop}}}\right)^{1/3}
  \label{eq:orbit_r}
\end{equation}

centered on the \textsc{Stop} drone's estimated position.  One partner
orbits clockwise and one counter-clockwise (start angles $0$ and
$\pi$), ensuring diverse angular measurements for the Particle Filter.

\subsection{Distributed Source Estimation: Particle Filter}
\label{sec:pf}

When a drone transitions to \textsc{Track} (or to \textsc{Support} without
prior detection), it initializes a 3-D Particle Filter with $N_p = 300$
particles $\{\bm{\xi}_k\}_{k=1}^{N_p}$ and uniform weights $w_k = 1/N_p$.

\paragraph{LiDAR ground constraint}
The victim is buried, so every particle must lie \emph{below} the terrain
surface.  The LiDAR altimeter provides the terrain height directly beneath
the drone, $z_{\text{ground}} = p_z - h_{\text{AGL}}$, and under the
locally-flat assumption this yields a hard physical prior on every particle:
\begin{equation}
  \xi_{k,z} \leq z_{\text{ground}} = p_z - h_{\text{AGL}}.
  \label{eq:pf_ground}
\end{equation}
The constraint is enforced both at initialization and after every
resampling jitter, so that the particle cloud never occupies the
physically impossible volume above the snow surface.

\paragraph{Initialization}
Particles are drawn in polar coordinates relative to the detecting
drone's position $\mathbf{p}$, on the shell of probable source positions
consistent with the first measurement.  The vertical depth of a particle
below the drone is $d = r_0\cos\psi$; imposing the ground
constraint~\eqref{eq:pf_ground}, i.e.\ $d \geq p_z - z_{\text{ground}} =
h_{\text{AGL}}$, gives
\begin{equation}
  r_0(\sin\psi)\,\cos\psi \geq h_{\text{AGL}},
  \quad
  r_0(\sin\psi) = \left(\frac{m\sqrt{1+3\cos^2\!\psi}}{S_0}\right)^{1/3}.
  \label{eq:pf_sinpsi_constraint}
\end{equation}
The left-hand side is monotonically decreasing in $\sin\psi$, hence the
constraint admits a unique upper bound $\sin\psi_{\max}$ (located by a fast
monotone scan) that restricts the elevation sampling interval.  For each
particle:
\begin{gather}
  \phi \sim \mathcal{U}[0, 2\pi],\quad
  \sin\psi \sim \mathcal{U}[0,\,\sin\psi_{\max}],\notag\\
  r_0 = \left(\frac{m\sqrt{1+3(1-\sin^2\!\psi)}}{S_0}\right)^{1/3},
  \label{eq:pf_init_polar}
\end{gather}
converted to Cartesian coordinates as
\begin{equation}
  \bm{\xi}_k = \mathbf{p} + r_0
  \begin{bmatrix} \cos\phi\,\sin\psi \\ \sin\phi\,\sin\psi \\
  -\cos\psi \end{bmatrix}.
  \label{eq:pf_init_cart}
\end{equation}
In the degenerate case where even a straight-down particle
($\sin\psi = 0$) cannot reach the ground—drone almost directly over the
source—$\sin\psi_{\max}$ collapses to $0$ and a final clamp to
$z_{\text{ground}}$ enforces~\eqref{eq:pf_ground}.

\paragraph{Weight update}
At each step the predicted signal at particle $k$ for the measuring
drone at position $\mathbf{p}$ is
$S_{\text{pred},k} = S(\mathbf{p}, \bm{\xi}_k)$ from~\eqref{eq:artva},
and the weight is updated with the ARTVA likelihood:
\begin{equation}
  w_k \leftarrow w_k \cdot \exp\!\left(
    -\frac{(S - S_{\text{pred},k})^2}{2\,\sigma_{\text{eff}}^2}
  \right),
  \label{eq:weight}
\end{equation}
with the adaptive noise model
\begin{equation}
  \sigma_{\text{eff}} = \sqrt{\sigma_n^2 + (0.20\,S)^2},
  \label{eq:sigma_eff}
\end{equation}
which adds a 20\,\% multiplicative component.  This is essential for
cooperative updates: drones orbiting near the source receive signals
orders of magnitude larger than distant drones, and a fixed $\sigma_n$
would collapse all weights to zero on cross-drone updates.  Weights are
then normalized:
\begin{equation}
  w_k \leftarrow \frac{w_k}{\sum_{j=1}^{N_p} w_j}.
  \label{eq:pf_norm}
\end{equation}

\paragraph{Resampling}
Systematic resampling is triggered only when the effective particle
count
\begin{equation}
  N_{\text{eff}} = \frac{1}{\sum_{k=1}^{N_p} w_k^2}
  \label{eq:neff}
\end{equation}
drops below $N_p/2$, preserving particle diversity near the source.
After resampling, independent Gaussian jitter is added to each particle:
\begin{equation}
  \bm{\xi}_k \leftarrow \bm{\xi}_k + \mathcal{N}(\mathbf{0},\,\mathrm{diag}(\bm{\sigma}_j^2)),
  \label{eq:pf_jitter}
\end{equation}
with $\bm{\sigma}_j = \max(0.1\,\hat{\bm{\sigma}},\,[1,1,0.5]^{\top})$\,m,
where $\hat{\bm{\sigma}}$ is the per-axis standard deviation of the current
particle cloud. This roughening prevents collapse when the cloud is already
concentrated near the source.
Since the vertical jitter can push particles above the terrain, the ground
constraint~\eqref{eq:pf_ground} is re-enforced afterwards by reflecting any
violating particle back below the surface:
\begin{equation}
  \xi_{k,z} \leftarrow 2\,z_{\text{ground}} - \xi_{k,z}
  \quad \text{if } \xi_{k,z} > z_{\text{ground}},
  \label{eq:pf_reflect}
\end{equation}
which preserves particle density immediately beneath the surface (a clamp
would instead pile probability mass exactly at $z_{\text{ground}}$).

\paragraph{Cooperative fusion}
Any drone $i$ with an active PF incorporates the ARTVA measurements of
all neighbors that also have an active PF and lie within $r_c$:
\begin{equation}
  \forall j \neq i : \|\mathbf{p}_i - \mathbf{p}_j\| < r_c
  \;\wedge\; \mathrm{PF}_j \text{ active}
  \;\Rightarrow\; \text{apply \eqref{eq:weight} with }
  (\mathbf{p}_j, S_j),
  \label{eq:pf_fusion}
\end{equation}
fusing diverse viewpoints without exchanging particle sets.  The source
estimate is the weighted mean of all particles:
\begin{equation}
  \hat{\bm{\theta}}_i = \sum_{k=1}^{N_p} w_k\,\bm{\xi}_k.
  \label{eq:pf_estimate}
\end{equation}

\paragraph{Estimate uncertainty and inter-drone consensus}
The point estimate~\eqref{eq:pf_estimate} alone does not convey how
\emph{confident} each drone is, nor whether the drones \emph{agree}.
We therefore characterise each drone's planar uncertainty by the
weighted $2\times2$ covariance of its particle cloud,
\begin{equation}
  \bm{\Sigma}_i = \sum_{k=1}^{N_p} w_k\,
  \bigl(\bm{\xi}_k^{xy} - \hat{\bm{\theta}}_i^{xy}\bigr)
  \bigl(\bm{\xi}_k^{xy} - \hat{\bm{\theta}}_i^{xy}\bigr)^{\!\top},
  \label{eq:pf_cov}
\end{equation}
whose $\alpha$-level confidence ellipse is the set
\begin{equation}
  \mathcal{E}_i = \Bigl\{\, \mathbf{x}\in\mathbb{R}^2 :
  (\mathbf{x}-\hat{\bm{\theta}}_i^{xy})^{\!\top}\bm{\Sigma}_i^{-1}
  (\mathbf{x}-\hat{\bm{\theta}}_i^{xy}) \le k^2 \,\Bigr\},
  \label{eq:pf_ellipse}
\end{equation}
with $k^2 = \chi^2_{2}(\alpha) = -2\ln(1-\alpha)$ the chi-square quantile
for two degrees of freedom (closed form for $2$ d.o.f.;
$\alpha = 0.95 \Rightarrow k^2 \approx 5.991$).  The area of this ellipse,
\begin{equation}
  A_i = \pi\,k^2\sqrt{\det \bm{\Sigma}_i},
  \label{eq:pf_area}
\end{equation}
measures the \emph{intra-drone} uncertainty: a smaller $A_i$ means a more
concentrated, more confident estimate.  Averaging over the $D$ drones with
an active PF gives the mean confidence-ellipse area $\bar{A} =
\frac{1}{D}\sum_{i} A_i$.

To quantify \emph{inter-drone} agreement we use the
intersection-over-union (IoU) of the confidence ellipses of every drone
pair $(i,j)$,
\begin{equation}
  \mathrm{IoU}_{ij} =
  \frac{\operatorname{area}(\mathcal{E}_i \cap \mathcal{E}_j)}
       {\operatorname{area}(\mathcal{E}_i \cup \mathcal{E}_j)}
  \in [0,1],
  \label{eq:pf_iou}
\end{equation}
computed exactly by convex-polygon clipping of the two ellipse contours,
and report its pairwise mean
\begin{equation}
  \overline{\mathrm{IoU}} = \frac{2}{D(D-1)}
  \sum_{i<j} \mathrm{IoU}_{ij}.
  \label{eq:pf_iou_mean}
\end{equation}
Unlike the spread of the estimate centroids, the IoU jointly captures
\emph{both} the disagreement between centres \emph{and} the magnitude of
each drone's uncertainty: two distant but very uncertain estimates still
overlap (they are mutually consistent), whereas two close but tight
estimates do not.  Crucially, normalising by the union rather than using
the raw intersection removes the geometric bias whereby larger ellipses
trivially overlap more.  The ideal operating regime is therefore
\emph{small} $\bar{A}$ together with \emph{high} $\overline{\mathrm{IoU}}$:
drones that are individually confident and mutually in agreement.

% ============================================================
\section{Implementation Details}
\label{sec:impl}

\subsection{Software Architecture}

The system is implemented as independent Python modules;
\texttt{config.py} is the single source of truth for all parameters.

\begin{center}
\small
\begin{tabular}{ll}
\toprule
Module & Role \\
\midrule
\texttt{config.py}      & Global parameters \\
\texttt{terrain.py}     & DEM I/O, \texttt{Terrain} class \\
\texttt{artva.py}       & ARTVA dipole signal model \\
\texttt{mpc\_drone.py}  & MPC controller + point-mass model \\
\texttt{imdcl.py}       & Cooperative Kalman localization \\
\texttt{pf.py}          & 3-D Particle Filter \\
\texttt{drone\_agent.py}& FSM, lawnmower, ES navigation \\
\texttt{simulation.py}  & Multi-agent loop \\
\texttt{main.py}        & CLI entry point \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Flight Controller: MPC}
\label{sec:mpc}

Each drone tracks its mission waypoints via a finite-horizon MPC with
horizon $N = 20$ steps and $\Delta t = 0.1$\,s.  At every control step
the current IMDCL estimate $\hat{\mathbf{x}}^+_i$ serves as the initial
condition, and the solver minimizes a quadratic tracking cost over the
predicted input sequence $\mathbf{u}_0, \ldots, \mathbf{u}_{N-1}$:
\begin{equation}
  \begin{aligned}
  \min_{\mathbf{u}_0, \ldots, \mathbf{u}_{N-1}}\;
  &\sum_{k=0}^{N-1} \Big( w_p \|\mathbf{p}_k - \mathbf{p}_{\text{ref}}\|^2
    + w_a \|\mathbf{u}_k\|^2 \Big) \\
  &\quad + w_{fp} \|\mathbf{p}_N - \mathbf{p}_{\text{ref}}\|^2
    + w_{fv} \|\mathbf{v}_N\|^2
  \end{aligned}
  \label{eq:mpc_cost}
\end{equation}
subject to the discrete dynamics~\eqref{eq:zoh} and the box constraints
\begin{equation}
  \|\mathbf{v}_k\|_\infty \leq v_{\max} = 3\,\text{m/s},\quad
  \|\mathbf{u}_k\|_\infty \leq a_{\max} = 6\,\text{m/s}^2,
  \label{eq:mpc_constraints}
\end{equation}
for all $k = 0, \ldots, N$.  Here $\mathbf{p}_k, \mathbf{v}_k$ are the
position and velocity components of the predicted state
$\mathbf{x}_k$, $\mathbf{p}_{\text{ref}}$ is the active reference
waypoint, and $(w_p, w_a, w_{fp}, w_{fv})$ are the stage-position,
control-effort, terminal-position, and terminal-velocity weights,
respectively.  The terminal velocity penalty $w_{fv}\|\mathbf{v}_N\|^2$
discourages overshoot at the waypoint.
The MPC always uses the IMDCL state estimate $\hat{\mathbf{x}}$,
never the true state, making it robust to GPS denial by design.
The target altitude is set from the terrain as
$z_{\text{tgt}} = z_{\text{terrain}}(x, y) + h_{\text{AGL}}$, keeping
the drone at a constant 1.5\,m above the DEM surface.
The problem is solved with the IPOPT interior-point solver through the
CasADi symbolic framework.

\paragraph{Warm-starting}
At mission start a single initialization solve runs up to
$N_{\text{init}} = 1000$ IPOPT iterations to obtain a high-quality
primal–dual solution.  All subsequent steps warm-start from the
previous solution: the optimal state trajectory is shifted by one step
to initialize the primal variables, and IPOPT is then limited to
$N_{\text{mpc}} = 3$ iterations per step, keeping the average solve time
below 5\,ms.

\paragraph{Waypoint sequencing}
When the estimated distance to the active waypoint drops below
$\delta = 0.3$\,m, the planner advances to the next target and updates
the reference without triggering a new initialization solve; the
shifted warm-start carries over, ensuring smooth transitions between
consecutive waypoints.

\subsection{GPS-Denied Localization: IMDCL}
\label{sec:imdcl}

In GPS-denied conditions each drone runs an instance of the IMDCL
filter~\cite{Kia2016}, a tightly-coupled distributed
cooperative localization algorithm that replicates the result of a
centralized EKF without all-to-all communication at every step.
The key insight is that the cross-covariance terms between agents $i$
and $j$, while never stored globally, can be reconstructed locally
whenever a relative measurement occurs.  To this end each agent
maintains four quantities: the state estimate $\hat{\mathbf{x}}_i$,
the error covariance $\mathbf{P}_i$ (initialized with position standard
deviation $\sqrt{P^{pp}_0} = 0.5$\,m), the cumulative Jacobian product
$\Phi_i$ (initialized to $I$ and multiplied by $F$ at each propagation
step), and local copies of the cross-covariance seed matrices
$\{\Pi^i_{jl}\}$.  The cross-covariance is then recovered as
$P_{ij}(k) = \Phi_i(k)\,\Pi^i_{ij}\,\Phi_j(k)^\top$ without additional
communication.

At each step the filter executes the following operations.

\paragraph{Propagation (every step)}
Every agent independently propagates its local filter and advances
$\Phi_i$, with no inter-agent communication:
\begin{align}
  \hat{\mathbf{x}}^-_i(k{+}1) &= F\,\hat{\mathbf{x}}^+_i(k) +
  G\,\mathbf{u}_i(k), \\
  \mathbf{P}^-_i(k{+}1) &= F\,\mathbf{P}^+_i(k)\,F^\top +
  G\,Q\,G^\top, \\
  \Phi_i(k{+}1) &= F\,\Phi_i(k).
  \label{eq:imdcl_prop}
\end{align}

\paragraph{Absolute update (LiDAR altitude)}
The LiDAR altimeter provides an absolute altitude measurement
($\sigma_{\text{LiDAR}} = 0.05$\,m), processed as a standard EKF
correction on the $p_z$ component.

\paragraph{Relative update (agent $a$ measures agent $b$)}
When agent $a$ obtains a UWB relative-position measurement
$z_{ab} = \mathbf{p}_b - \mathbf{p}_a + \nu_{ab}$ of an in-range
neighbor $b$ ($\sigma_{\text{rel}} = 0.3$\,m), it is declared the
interim master.  It requests the landmark message
$(\hat{\mathbf{x}}^-_b, \Phi_b, \mathbf{P}^-_b)$ and computes the
innovation and innovation covariance:
\begin{align}
  r^a &= z_{ab} - h_{ab}(\hat{\mathbf{x}}^-_a, \hat{\mathbf{x}}^-_b),\\
  S_{ab} &= R^a + \tilde{H}_a \mathbf{P}^-_a \tilde{H}_a^\top
    + \tilde{H}_b \mathbf{P}^-_b \tilde{H}_b^\top \nonumber\\
    &\quad - \tilde{H}_a \Phi_a \Pi^a_{ab} \Phi_b^\top \tilde{H}_b^\top
    - (\cdot)^\top,
  \label{eq:imdcl_innov}
\end{align}
where $\tilde{H}_a = [-I_3\ 0_3]$ and $\tilde{H}_b = [I_3\ 0_3]$.
The gain factors for the measuring agent $a$ and the landmark $b$ are:
\begin{align}
  \Gamma_a &= \left( \Pi^a_{ab} \Phi_b^\top \tilde{H}_b^\top
    - \Phi_a^{-1} \mathbf{P}^-_a \tilde{H}_a^\top \right) S_{ab}^{-1/2},
  \label{eq:imdcl_gamma_a}\\
  \Gamma_b &= \left( \Phi_b^{-1} \mathbf{P}^-_b \tilde{H}_b^\top
    - \Pi^b_{ba} \Phi_a^\top \tilde{H}_a^\top \right) S_{ab}^{-1/2}.
  \label{eq:imdcl_gamma_b}
\end{align}
Agent $a$ packs $\big(\Gamma_a, \Gamma_b,
\Phi_b^\top \tilde{H}_b^\top S_{ab}^{-1/2},
\Phi_a^\top \tilde{H}_a^\top S_{ab}^{-1/2}\big)$ into the update message
and broadcasts it to the whole reachable team.  Every other agent
$i \notin \{a,b\}$ derives its own gain locally from its $\Pi$ copies:
\begin{equation}
  \Gamma_i = \Pi^i_{ib} \Phi_b^\top \tilde{H}_b^\top S_{ab}^{-1/2}
    - \Pi^i_{ia} \Phi_a^\top \tilde{H}_a^\top S_{ab}^{-1/2}.
  \label{eq:imdcl_gamma_i}
\end{equation}
Finally every agent $i \in \mathcal{V}$ applies the correction:
\begin{align}
  \hat{\mathbf{x}}^+_i &= \hat{\mathbf{x}}^-_i + \Phi_i \Gamma_i
  \tilde{r}^a, \label{eq:imdcl_state}\\
  \mathbf{P}^+_i &= \mathbf{P}^-_i - \Phi_i \Gamma_i \Gamma_i^\top
  \Phi_i^\top, \label{eq:imdcl_cov}\\
  \Pi^i_{jl}(k{+}1) &= \Pi^i_{jl}(k) - \Gamma_j \Gamma_l^\top,
  \label{eq:imdcl_seed}
\end{align}
where $\tilde{r}^a = S_{ab}^{-1/2} r^a$.  The seed update
\eqref{eq:imdcl_seed} propagates the measurement benefit to all
cross-covariance pairs, and $\Phi_i$ is reset to $I$ after the update.

\begin{figure}[tb]
  \centering
  \includegraphics[width=\columnwidth]{figures/imdcl_error}
  \caption{IMDCL localization error over time for a 3-drone mission.
  Cooperative inter-drone updates (steps with neighbor in range)
  produce visible error reductions.}
  \label{fig:imdcl}
\end{figure}

\subsection{Terrain and Simulation Environment}

Experiments run on real DEM data from the TINItaly dataset (10\,m
resolution tile \texttt{w51065\_s10.tif}, Trentino Alps).
The \texttt{simulation.py} module runs a synchronous multi-agent loop
at 10\,Hz: at each tick every drone queries the terrain height,
computes its MPC command, updates IMDCL, reads the ARTVA signal, and
exchanges messages with in-range neighbors.
The CLI in \texttt{main.py} exposes all parameters for batch sweeps.

% ============================================================
\section{Results}
\label{sec:results}

\subsection{Experimental Setup}

Five 100\,m\,$\times$\,100\,m patches were extracted from the TINItaly
tile.  The parametric grid covers:

\begin{center}
\small
\begin{tabular}{ll}
\toprule
Parameter & Values \\
\midrule
Fleet size $N$           & $\{3, 4\}$ drones \\
ARTVA noise $\sigma_n$   & $\{10^{-7}, 10^{-6}, 10^{-5}\}$ \\
Process noise $\sigma_a$ & $\{0.05, 0.10\}$\,m/s$^2$ \\
Comm.\ radius $r_c$      & $\{25, 60, 120\}$\,m \\
Terrain patches          & 5 distinct DEM tiles \\
Victim positions         & 10 random per combination \\
\bottomrule
\end{tabular}
\end{center}

Each run terminates at success ($\geq 3$ drones in \textsc{Stop}
\emph{and} estimate within 10\,m) or after a 900\,s timeout.
The total sweep comprised 2\,153 completed experiments.

\subsection{Mission Success Rate}

\begin{table}[tb]
  \caption{Overall mission success rate by fleet size}
  \label{tab:success}
  \centering
  \small
  \begin{tabular}{ccc}
    \toprule
    $N$ & Runs & Success \\
    \midrule
    3   & 1200 & 80.4\,\% \\
    4   &  953 & 92.2\,\% \\
    \midrule
    All & 2153 & 85.6\,\% \\
    \bottomrule
  \end{tabular}
\end{table}

Table~\ref{tab:success} reports success rates by fleet size.
The fourth drone provides a meaningful gain (80.4\,\% $\to$ 92.2\,\%),
increasing the probability that at least one drone reaches the victim
and two others can be recruited as \textsc{Support}.

\subsection{Effect of Noise Level}

\begin{table}[tb]
  \caption{Success rate by noise level}
  \label{tab:noise}
  \centering
  \small
  \begin{tabular}{ccc}
    \toprule
    $\sigma_n$ & Runs & Success \\
    \midrule
    $10^{-7}$ & 720 & 87.1\,\% \\
    $10^{-6}$ & 715 & 84.3\,\% \\
    $10^{-5}$ & 718 & 85.5\,\% \\
    \bottomrule
  \end{tabular}
\end{table}

Table~\ref{tab:noise} shows nearly uniform success across three decades
of noise, confirming that the adaptive thresholds effectively decouple
mission performance from noise level.

\subsection{Effect of Communication Radius}

\begin{table}[tb]
  \caption{Success rate by communication radius}
  \label{tab:rc}
  \centering
  \small
  \begin{tabular}{ccc}
    \toprule
    $r_c$ [m] & Runs & Success \\
    \midrule
    25  & 540 & 75.9\,\% \\
    60  & 540 & 88.7\,\% \\
    120 & 540 & 92.0\,\% \\
    \bottomrule
  \end{tabular}
\end{table}

Table~\ref{tab:rc} shows the expected trend: a larger communication
radius enables cooperative PF updates from more diverse viewpoints,
improving both IMDCL accuracy and particle filter convergence.
At $r_c = 25$\,m some missions fail because no neighbor is reachable
for UWB cooperative updates during the lawnmower phase.

\begin{figure}[tb]
  \centering
  \includegraphics[width=\columnwidth]{figures/artva_signal}
  \caption{ARTVA signal (smoothed) over time for each drone.
  Horizontal dashed lines mark $\tau_{\text{detect}}$ and
  $\tau_{\text{stop}}$.  State transitions coincide with signal
  crossings.}
  \label{fig:signal}
\end{figure}

\begin{figure}[tb]
  \centering
  \includegraphics[width=\columnwidth]{figures/final_positions}
  \caption{Final drone positions and PF source estimate (diamond) vs.\
  true victim (star).  Each drone's shaded $95\%$ confidence ellipse
  (\eqref{eq:pf_ellipse}) visualises its planar uncertainty; the inset
  reports the mean ellipse area $\bar{A}$~\eqref{eq:pf_area} and the mean
  pairwise IoU $\overline{\mathrm{IoU}}$~\eqref{eq:pf_iou_mean}.  Tightly
  overlapping ellipses near the victim indicate inter-drone consensus.}
  \label{fig:final}
\end{figure}

\subsection{Localization Accuracy and Search Time}

Among successful runs, the median search time is \textbf{105.5\,s}
with a median 2-D position error of \textbf{1.76\,m}, well within the
10\,m operational requirement for rescuers to probe the exact burial
point.

Beyond the point-estimate error, we assess the \emph{consistency} of the
distributed estimate through the confidence-ellipse metrics of
Section~\ref{sec:pf}.  The mean ellipse area $\bar{A}$~\eqref{eq:pf_area}
quantifies how concentrated each drone's particle cloud is, while the
mean pairwise Intersection over Union $\overline{\mathrm{IoU}}$~\eqref{eq:pf_iou_mean}
quantifies inter-drone agreement.  As illustrated in
Fig.~\ref{fig:final}, the drones recruited near the victim produce
tightly overlapping ellipses (high IoU), confirming that cooperative
fusion~\eqref{eq:pf_fusion} drives the team toward a shared, confident
estimate rather than merely toward coincidentally close centroids.

% ============================================================
\section{Conclusions}
\label{sec:conclusion}

In this report we presented a fully distributed multi-drone ARTVA
search system operating over real alpine terrain, without any central
coordinator.
The combination of dynamic thresholds with a physics-based floor,
Extremum Seeking for gradient-free convergence, a cooperative 3-D
Particle Filter with adaptive likelihood, and IMDCL for GPS-free
localization achieves an 85.6\,\% mission success rate with a median
localization error of 1.76\,m across 2\,153 randomized experiments.

The main failure mode (19.6\,\% with $N=3$) is the inability to
recruit two \textsc{Support} partners when the fleet is small and
drones scatter.  Adding a fourth drone reduces this to 7.8\,\%.
The adaptive lawnmower and dynamic thresholds make the system robust
to noise over three decades with negligible performance variation.

Future directions include: extending the sweep to 200\,m tiles and
five drones; studying victim depth estimation accuracy; and replacing
simulated UWB/LiDAR with realistic sensor noise models including
multipath and terrain occlusion.

% ============================================================
\begin{thebibliography}{9}

\bibitem{Falk2011}
M.~Falk, H.~Brugger, and L.-E.~Adler-Kastner,
``Avalanche survival chances,''
\emph{Nature}, vol.\ 368, pp.\ 21--21, 1994.

\bibitem{Azzollini2021}
I.~A.~Azzollini, N.~Mimmo, L.~Gentilini, and L.~Marconi,
``UAV-Based Search and Rescue in Avalanches using ARVA:
  An Extremum Seeking Approach,''
\emph{arXiv preprint arXiv:2106.14514}, 2021.

\bibitem{Kia2016}
S.~S.~Kia, S.~Rounds, and S.~Mart\'inez,
``Cooperative localization for mobile agents: A recursive decentralized
  algorithm based on Kalman-filter decentralization,''
\emph{IEEE Control Syst.\ Mag.}, vol.\ 36, no.\ 2, pp.\ 86--101, 2016.

\end{thebibliography}

\end{document}